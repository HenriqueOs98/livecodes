import Component, { IComponentOptions } from '../share/Component';
import types from 'licia/types';
export interface IOptions extends IComponentOptions {
    node?: ChildNode;
    ignore?: types.AnyFn;
    parent?: DomViewer | null;
    isEndTag?: boolean;
    rootContainer?: HTMLElement;
    rootDomViewer?: DomViewer;
}
export default class DomViewer extends Component<IOptions> {
    private $tag;
    private $children;
    private isExpanded;
    private observer;
    private childNodes;
    private childNodeDomViewers;
    private endTagDomViewer?;
    constructor(container: HTMLElement, options?: IOptions);
    select(node?: ChildNode): void;
    attach(): void;
    isAttached(): boolean;
    detach(): void;
    expand(): void;
    collapse(): void;
    toggle: () => void;
    destroy(): void;
    private renderExpandTag;
    private renderCollapseTag;
    private initObserver;
    private handleMutation;
    private bindEvent;
    private isExpandable;
    private getChildNodes;
    private initTpl;
    private renderChildNodes;
    private renderHtmlTag;
    private renderTextNode;
    private renderHtmlComment;
}
