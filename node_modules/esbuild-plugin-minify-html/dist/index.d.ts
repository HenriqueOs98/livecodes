import { Plugin } from 'esbuild';
import { Options } from 'html-minifier-terser';

type MinifyHTMLOptions = Options;
declare const minifyHtmlPlugin: (options?: Options) => Plugin;

export { MinifyHTMLOptions, minifyHtmlPlugin as default };
