import Component, { IComponentOptions } from '../share/Component';
import types from 'licia/types';
export interface IColumn {
    id: string;
    title: string;
    weight?: number;
    sortable?: boolean;
    comparator?: types.AnyFn;
}
export interface IOptions extends IComponentOptions {
    columns: IColumn[];
    height?: number;
    maxHeight?: number;
    minHeight?: number;
    filter?: string | RegExp | types.AnyFn;
}
export interface IDataGridNodeOptions {
    selectable?: boolean;
}
export default class DataGrid extends Component<IOptions> {
    private $headerRow;
    private $fillerRow;
    private fillerRow;
    private $tableBody;
    private $colgroup;
    private $dataContainer;
    private $resizers;
    private resizeIdx;
    private resizeStartX;
    private resizeStartLeft;
    private resizeDeltaX;
    private resizeSensor;
    private onResize;
    private tableBody;
    private nodes;
    private colWidthsInitialized;
    private colMap;
    private sortId?;
    private selectedNode;
    private isAscending;
    private colWidths;
    constructor(container: HTMLElement, options: IOptions);
    destroy(): void;
    remove(node: DataGridNode): void;
    append(data: types.PlainObj<string | HTMLElement>, options?: IDataGridNodeOptions): DataGridNode;
    clear(): void;
    private updateHeight;
    private selectNode;
    private onResizeColStart;
    private onResizeColMove;
    private onResizeColEnd;
    private bindEvent;
    private sortNodes;
    private updateWeights;
    private applyColWeights;
    private positionResizers;
    private renderData;
    private filterNode;
    private renderHeader;
    private renderResizers;
    private initTpl;
}
export declare class DataGridNode {
    container: HTMLElement;
    data: types.PlainObj<string | HTMLElement>;
    selectable: boolean;
    private $container;
    private dataGrid;
    constructor(dataGrid: DataGrid, data: types.PlainObj<string | HTMLElement>, options?: IDataGridNodeOptions);
    text(): string;
    detach(): void;
    select(): void;
    deselect(): void;
    render(): void;
}
